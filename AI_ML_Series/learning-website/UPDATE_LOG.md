# Learning website checkpoint — 2026-09-05

## Resume from here

Previous website checkpoint: **2026-09-03**, commit `0c7208e`, with **47 concepts and 85 mapped learning files**. Its latest lessons were Naive Bayes and hyperparameter tuning. The intervening tracked evidence is the ensemble notebook and two handwritten sheets; `21_Outliers.ipynb` is additional local evidence that was untracked when this update began.

Current checkpoint: **57 concepts and 89 mapped learning files**, including **32 notebooks**, **14 dataset files**, **2 Python files**, **24 Markdown sources**, **7 web learning assets**, **7 source images**, **2 diagram sources**, and **1 requirements file**. These counts concern original learning evidence, not generated website assets or percentage completion of ML.

**Continue with Local Outlier Factor:** compare `(7, 1)` versus `(7, 2)`, then change `k=3` to `k=6`. The next unfinished experiment is a proper train/test evaluation of RandomForestRegressor. Naive Bayes retains its existing warning about the unresolved MultinomialNB execution order. Do not automatically mark entire course modules complete.

## Incremental changes

- Added Random Forest/bagging, Random Forest regression, AdaBoost, gradient boosting, and hard/soft voting to the existing classification/regression branches.
- Added outlier investigation, DBSCAN, Elliptic Envelope, Isolation Forest, and LOF to the existing unsupervised branch.
- Preserved all 47 earlier concepts and all 85 earlier source mappings. The coefficient-by-hand notebook was already linked; it now has a complete five-row arithmetic table, contribution visualization, and original plots.
- Mapped the two Naive Bayes/hyperparameter handwritten sheets and exposed source images in the concept panels. Bayesian optimization, Hyperband, calibration and SVC/KNN internals remain references rather than newly completed lessons.
- Connected tree classification to forests/AdaBoost, tree regression to forests/gradient boosting, K-Means to DBSCAN, preprocessing to outliers/LOF, and tuning to ensemble settings.
- Added formula symbol explanations, parameter tables, comparisons, worked examples, check-yourself questions, original plot galleries and full row inspection.
- Added a resume panel, current-learning items to the recent rail, accurate source statistics, and a source-hash inventory for the next update.
- Removed arbitrary half-credit for in-progress concepts from the branch coverage bars; bars now show the fraction of mapped concepts marked learned.
- Updated the root `ROADMAP.html`, whose current-lesson labels and future-work lists still stopped at clustering. Existing HTML entry points and the local static-file architecture are preserved.

## All data points and provenance

`data-explorer.html` contains all **12,662 rows** across six dataset views:

| Data | Rows | Source and provenance |
|---|---:|---|
| Three clouds | 500 | `21_Outliers.ipynb`, `make_blobs`, centers 3, spread 0.4, seed 0 |
| Two clouds | 2,000 | Same notebook, centers 2, spread 0.9, seed 0 |
| LOF requests | 7 | Literal rows A–G in the notebook |
| Iris flowers | 150 | `20_Ensemble_methods.ipynb`, sklearn bundled Iris; all four features available |
| Forest regression | 10,000 | Same notebook, `make_regression`, one feature, noise 2, seed 0 |
| Salary arithmetic | 5 | Literal rows in `05a_Coef_And_Intercept_By_Hand.ipynb` |

Every matching row is plotted; tables paginate 25 rows at a time without sampling the plot. Axis selectors expose all columns, row selection displays full numeric values, source-group filters preserve source row IDs, and CSV downloads include all matching rows.

`lesson-data.js` is generated by `export_lesson_data.py` using installed **scikit-learn 1.9.0 / NumPy 2.4.6**. It preserves full numeric precision in storage. It also exports **21 original saved PNG charts**, without executing or modifying the learner notebooks.

Numerical checks: DBSCAN radii 0.15/0.3/0.6 produce 256/25/0 noise rows; baseline counts are 415 core, 60 border, 25 noise. LOF F/G scores at k=3 are approximately 3.611/4.400 with one feature and 3.316/4.139 with two. At k=6 on both features, neither is flagged. The reproducible ellipse flags 200/2,000 rows.

The ellipse replay adds `random_state=0` to the originally unseeded estimator; it is explicitly distinguished from its original saved plot. Isolation scores reproduce the notebook's separately seeded experiment. Interactive tree voting, weight rebalancing and scalar gradient corrections are labeled **teaching illustrations**, not reconstructed training runs. Saved soft-vote row-0 probabilities are identified separately. Member scores are not claimed as a saved final ensemble score. Forest regression remains **currently learning**, since 0.99117 is training R² only.

## Traceability and boundaries

Every active learning artifact is mapped. Instructions are excluded from learning counts. A recursive repository scan also inspected earlier archive materials as prior evidence; their older work does not change current-course statuses.

The outlier notebook refers to `IMG_3446.jpeg`, `IMG_3447.jpeg`, and `IMG_3448.jpeg`, but those files are **not present** in this repository. Their notebook explanations are mapped; no nonexistent image link or inspection claim is added. No other active learning file remains uncategorized.

## Next update procedure

1. Read `CODEX_LEARNING_PROMPT.md` and this checkpoint.
2. Compare current learning-file SHA-256 values against `source-inventory.json`; inspect content changes, not filenames alone.
3. Merge into existing concept IDs. Preserve prior evidence and current/reference statuses unless new work supports a change.
4. If these data/plots change, run `pizza_env/bin/python AI_ML_Series/learning-website/export_lesson_data.py` from the repository root.
5. Verify formulas, interactions, file links and offline loading, then refresh `source-inventory.json` with `python3 AI_ML_Series/learning-website/refresh_inventory.py`.
6. Record the new checkpoint here. Neither workflow instruction file should be changed.
